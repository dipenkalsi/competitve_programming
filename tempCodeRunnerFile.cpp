sort(v.begin(), v.end());
        // int ptr1 = 0, ptr2 = v.size()-1;
        // for(int i=0; i<k; i++){
        //     if(v[ptr1]+v[ptr1+1]<=v[ptr2]){
        //         ptr1+=2;
        //     }
        //     else ptr2--;
        // }
        // cout<<accumulate(v.begin()+ptr1, v.end()+ptr2, 0)<<endl;