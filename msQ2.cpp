#include<bits/stdc++.h>
using namespace std;
 
int main(){
    int n;
    cin>>n;
    vector<char> starts, ends;
    string ans = "";
    for(int i=0; i<n; i++){
        string s;
        cin>>s;
        if(i==0){
            starts.push_back(s[0]);
            ends.push_back(s[1]);
        }
        else{
            for(auto)
        }
    }
    return 0;
}